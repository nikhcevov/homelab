# Managed by Ansible (role: fail2ban). Source: group_vars/vps/security.yml
[DEFAULT]
bantime  = {{ fail2ban_bantime }}
findtime = {{ fail2ban_findtime }}
maxretry = {{ fail2ban_maxretry }}
backend  = systemd

[sshd]
enabled = true
port    = {{ sshd_port }}
