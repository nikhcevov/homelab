# Managed by Ansible (role: openwrt_upgrades). Source: group_vars/routers/upgrades.yml
NTFY_URL="{{ ntfy_url }}"
NTFY_TOPIC_ALERTS="{{ ntfy_topic_alerts }}"
HOST_PREFIX="[{{ owrt_hostname | default(inventory_hostname) }}]"
OWRT_OWUT_CHECK={{ '1' if owrt_owut_check | bool else '0' }}
